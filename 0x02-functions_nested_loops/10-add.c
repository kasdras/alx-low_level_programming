#include "main.h"
#include <stdio.h>

/**
 * add - checks for alphabetic character
 *
 * int: it is a sign
 * @p: it is a value
 * @c: a value
 *
 * Return: Always 0 Success
 */
int add(int p, int c)
{
	int m;

	m = p + c;
	return (m);
}
